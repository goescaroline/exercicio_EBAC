function multiplicacao(x: number, y: number): number {
    return x * y;
}
console.log(multiplicacao(10, 15));

function dizOi(nome: string): string {
    return `Olá, ${nome}`; 
}
console.log(dizOi("Maria"));
